class Solution {
    public int findContentChildren(int[] g, int[] s) {
        int c=0;
        Arrays.sort(g);
        Arrays.sort(s);
        for(int i=0;c<g.length && i<s.length;++i){
            if (g[c]<=s[i]){
                c++;
            }
        }
        return c;
    }
}
